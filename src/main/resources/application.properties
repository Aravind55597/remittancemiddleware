spring.profiles.active=${CODE_ENVIRONMENT}
sandbox.password=${SANDBOX_PASSWORD}}
sandbox.username=${SANDBOX_USERNAME}}
sandbox.base.url=${SANDBOX_BASE_URL}}
#spring.datasource.url=jdbc:mysql://${LOCAL_HOSTNAME}:${LOCAL_PORT}/${LOCAL_NAME}&serverTimezone=UTC
#spring.datasource.username=${LOCAL_USERNAME}
#spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO}
#server.port=${SERVER_PORT}

